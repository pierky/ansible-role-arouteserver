---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

script:
  - "cd tests ; ansible-playbook -i tests/inventory tests/test.yml --syntax-check"
