---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible
  - "printf 'roles_path = /home/travis/build/pierky' > ansible.cfg"
    
script:
  - ansible-playbook -i tests/inventory tests/test.yml --syntax-check
